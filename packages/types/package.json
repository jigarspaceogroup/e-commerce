{
  "name": "@repo/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'types build placeholder'",
    "lint": "echo 'types lint placeholder'",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  }
}
