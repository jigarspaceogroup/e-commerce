// Re-export paths for convenience
// ESLint configs are in ./eslint/*.mjs (imported directly as ESM)
// Prettier config is in ./prettier.mjs (imported directly as ESM)
// TSConfig files are in ../tsconfig/*.json (referenced via extends)

export {};
